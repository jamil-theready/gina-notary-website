---
phase: 03-meta-descriptions
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/about/page.tsx
  - src/app/service-areas/page.tsx
  - src/app/notary-services/page.tsx
  - content/services/legal-personal.md
autonomous: true
requirements:
  - META-01
  - META-02
  - META-03
  - META-04

must_haves:
  truths:
    - "About page meta description is between 150 and 160 characters"
    - "Service areas landing meta description is between 150 and 160 characters"
    - "Notary services landing meta description is between 150 and 160 characters"
    - "Legal/personal service page meta description is between 150 and 160 characters"
  artifacts:
    - path: "src/app/about/page.tsx"
      provides: "Trimmed About page metadata"
      contains: "description:"
    - path: "src/app/service-areas/page.tsx"
      provides: "Trimmed service-areas landing metadata"
      contains: "description:"
    - path: "src/app/notary-services/page.tsx"
      provides: "Trimmed services landing metadata"
      contains: "description:"
    - path: "content/services/legal-personal.md"
      provides: "Trimmed legal/personal service frontmatter"
      contains: "metaDescription:"
  key_links:
    - from: "content/services/legal-personal.md"
      to: "src/app/notary-services/[slug]/page.tsx"
      via: "gray-matter frontmatter parsing"
      pattern: "metaDescription"
---

<objective>
Trim four meta descriptions that currently exceed 160 characters down to the 150-160 character sweet spot. These are all truncation edits — no expansion needed, just precise cuts while preserving keyword signal.

Purpose: Google truncates descriptions longer than ~160 characters in SERPs, wasting indexable copy. Trimmed descriptions display in full and signal relevance.
Output: Four files with corrected description strings, each confirmed at 150-160 chars.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Meta description pattern used across all pages -->
<!-- Pages use Next.js Metadata export: -->
```typescript
export const metadata: Metadata = {
  title: "...",
  description: "...",   // ← this is the field to edit
  alternates: { canonical: "..." },
};
```

<!-- Markdown service files use YAML frontmatter: -->
```yaml
---
metaDescription: "..."   # ← this is the field to edit
---
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trim About and service-areas landing meta descriptions</name>
  <files>src/app/about/page.tsx, src/app/service-areas/page.tsx</files>
  <action>
In src/app/about/page.tsx, replace the description value (per META-01):

CURRENT (167 chars):
"Former lawyer from Peru, now Sacramento's trusted mobile notary. 10+ years experience, 6,000+ documents, NNA certified, bilingual English/Spanish. Call (415) 948-9967."

REPLACE WITH (150 chars):
"NNA certified mobile notary in Sacramento. Former Peruvian lawyer, bilingual English/Spanish, 6,000+ documents notarized. Call Gina at (415) 948-9967."

---

In src/app/service-areas/page.tsx, replace the description value (per META-02):

CURRENT (179 chars):
"Gina Gonzalez is a mobile notary serving Sacramento, Elk Grove, Roseville, Folsom, Rancho Cordova, Davis, and all surrounding communities in Sacramento, Placer, and Yolo counties."

REPLACE WITH (156 chars):
"Mobile notary serving Sacramento, Elk Grove, Roseville, Folsom, Rancho Cordova, Davis, and surrounding communities in Sacramento, Placer, and Yolo counties."

Do not change any other metadata field (title, canonical URL) or any JSX. Only the description string.
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const about = fs.readFileSync('src/app/about/page.tsx','utf8').match(/description:\s*\n?\s*\"([^\"]+)\"/)?.[1] || '';
const sa = fs.readFileSync('src/app/service-areas/page.tsx','utf8').match(/description:\s*\n?\s*\"([^\"]+)\"/)?.[1] || '';
const aLen = about.length; const saLen = sa.length;
console.log('About desc length:', aLen, aLen>=150&&aLen<=160?'PASS':'FAIL');
console.log('ServiceAreas desc length:', saLen, saLen>=150&&saLen<=160?'PASS':'FAIL');
if(aLen<150||aLen>160||saLen<150||saLen>160) process.exit(1);
" 2>&1
    </automated>
  </verify>
  <done>About description is 150 chars and service-areas description is 156 chars — both in 150-160 range.</done>
</task>

<task type="auto">
  <name>Task 2: Trim services landing and legal/personal meta descriptions</name>
  <files>src/app/notary-services/page.tsx, content/services/legal-personal.md</files>
  <action>
In src/app/notary-services/page.tsx, replace the description value (per META-03):

CURRENT (170 chars):
"Mobile notary services in Sacramento. Legal documents, real estate, loan signings, apostille, wedding officiant, and more. Bilingual English/Spanish. Call (415) 948-9967."

REPLACE WITH (153 chars):
"Mobile notary services in Sacramento, CA. Legal documents, real estate, loan signings, apostille, wedding officiant, and more. Bilingual English/Spanish."

---

In content/services/legal-personal.md, replace the metaDescription frontmatter value (per META-04):

CURRENT (174 chars):
"Mobile notary for power of attorney, healthcare directives, affidavits, name changes, and prenuptial agreements in Sacramento. Bilingual English/Spanish. Call (415) 948-9967."

REPLACE WITH (152 chars):
"Mobile notary for power of attorney, healthcare directives, affidavits, and prenuptial agreements in Sacramento. Bilingual service. Call (415) 948-9967."

The legal-personal.md change is in the YAML frontmatter block (lines 1-25), not the markdown body. Only the metaDescription value changes — leave all other frontmatter fields untouched.
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const svc = fs.readFileSync('src/app/notary-services/page.tsx','utf8').match(/description:\s*\n?\s*\"([^\"]+)\"/)?.[1] || '';
const matter = fs.readFileSync('content/services/legal-personal.md','utf8').match(/metaDescription:\s*\"([^\"]+)\"/)?.[1] || '';
const sLen = svc.length; const mLen = matter.length;
console.log('Services desc length:', sLen, sLen>=150&&sLen<=160?'PASS':'FAIL');
console.log('Legal/personal desc length:', mLen, mLen>=150&&mLen<=160?'PASS':'FAIL');
if(sLen<150||sLen>160||mLen<150||mLen>160) process.exit(1);
" 2>&1
    </automated>
  </verify>
  <done>Services landing description is 153 chars and legal/personal frontmatter description is 152 chars — both in 150-160 range.</done>
</task>

</tasks>

<verification>
After both tasks, run the combined check:

```
node -e "
const fs = require('fs');
const files = [
  ['src/app/about/page.tsx', /description:\s*\n?\s*\"([^\"]+)\"/],
  ['src/app/service-areas/page.tsx', /description:\s*\n?\s*\"([^\"]+)\"/],
  ['src/app/notary-services/page.tsx', /description:\s*\n?\s*\"([^\"]+)\"/],
  ['content/services/legal-personal.md', /metaDescription:\s*\"([^\"]+)\"/],
];
let pass = true;
files.forEach(([f, re]) => {
  const desc = fs.readFileSync(f,'utf8').match(re)?.[1] || '';
  const ok = desc.length >= 150 && desc.length <= 160;
  console.log((ok?'PASS':'FAIL'), f, desc.length, 'chars');
  if (!ok) pass = false;
});
process.exit(pass ? 0 : 1);
"
```

All four must print PASS.
</verification>

<success_criteria>
- About page description: 150 chars (was 167)
- Service-areas landing description: 156 chars (was 179)
- Notary services landing description: 153 chars (was 170)
- Legal/personal frontmatter description: 152 chars (was 174)
- All four within 150-160 char range
- No other content modified in any of the four files
</success_criteria>

<output>
After completion, create `.planning/phases/03-meta-descriptions/03-01-SUMMARY.md`
</output>
