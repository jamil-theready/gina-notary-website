---
phase: 01-critical-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/not-found.tsx
autonomous: true
requirements:
  - CRIT-01
must_haves:
  truths:
    - "Visiting any non-existent URL (e.g., /does-not-exist) shows a branded page — not a blank screen or Next.js default error"
    - "The 404 page displays a heading that communicates the page was not found"
    - "The 404 page contains a link back to the homepage (/)"
    - "The 404 page uses the site's brand colors: gold (#f9cf01) and black (#0a0a0a)"
    - "The 404 page renders inside the global layout (Header and Footer are visible)"
  artifacts:
    - path: "src/app/not-found.tsx"
      provides: "App Router not-found page rendered for all unmatched routes"
      exports: ["default function NotFound"]
  key_links:
    - from: "src/app/not-found.tsx"
      to: "src/app/layout.tsx"
      via: "App Router automatic wrapping — not-found.tsx renders inside RootLayout"
      pattern: "export default function NotFound"
---

<objective>
Create a branded 404 not-found page using the App Router `not-found.tsx` convention.

Purpose: The site currently has no custom 404 page. Search engines and users who land on a broken URL see a blank page, which signals a low-quality site and causes users to abandon. This fixes CRIT-01.

Output: `src/app/not-found.tsx` — a server component that renders a branded, on-theme 404 experience with a clear path back to the homepage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create branded not-found.tsx page</name>
  <files>src/app/not-found.tsx</files>

  <read_first>
    - src/app/layout.tsx — to understand what Header/Footer wrap, the pt-[60px] md:pt-[96px] main padding, and brand class names
    - src/app/about/page.tsx — to understand the typical page structure and section/container patterns used across the site
    - src/app/globals.css — to confirm available brand color classes (bg-brand-gold, text-brand-black, bg-brand-gray-50, etc.)
  </read_first>

  <action>
Create `src/app/not-found.tsx` as a server component (no "use client" directive needed).

The file must:

1. Import `Link` from `"next/link"` (no other imports required — no framer-motion, no client hooks).

2. Export a default function named `NotFound`.

3. Render a single `<section>` with these exact Tailwind classes:
   `className="bg-brand-gray-50 flex flex-col items-center justify-center text-center px-6 py-32 sm:py-48"`

4. Inside that section, render this structure:

```tsx
<p className="text-brand-gold text-sm font-semibold tracking-widest uppercase mb-4">
  404
</p>
<h1 className="font-manrope text-4xl sm:text-5xl font-bold text-brand-black mb-6">
  Page Not Found
</h1>
<p className="text-brand-black/70 text-lg max-w-md mb-10">
  Sorry, we couldn&apos;t find the page you&apos;re looking for. It may have moved or never existed.
</p>
<Link
  href="/"
  className="inline-block bg-brand-gold text-brand-black font-semibold px-8 py-3 rounded-lg hover:brightness-95 transition-all"
>
  Back to Homepage
</Link>
```

5. Do NOT include `export const metadata` — Next.js App Router applies a default "404" title automatically to not-found pages. Adding metadata here causes a TypeScript error in this convention.

6. Do NOT add "use client" — this is a server component.

The file must compile without errors. Run `npm run lint` after creating it and fix any issues before marking done.
  </action>

  <verify>
    <automated>cd /Users/admin/Desktop/Clients/Gina\ Notary/website && npm run lint 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - File exists at `src/app/not-found.tsx`
    - File contains `export default function NotFound`
    - File contains `href="/"` (link back to homepage)
    - File contains `bg-brand-gold` (brand color applied)
    - File contains `Page Not Found` (visible heading text)
    - File does NOT contain `"use client"`
    - `npm run lint` exits with no errors related to `not-found.tsx`
  </acceptance_criteria>

  <done>
    `src/app/not-found.tsx` exists, exports a default `NotFound` function, contains a link to `/`, uses brand gold color, and passes lint. Any unmatched URL will render this page inside the global layout.
  </done>
</task>

</tasks>

<verification>
After task completion, confirm:
1. `src/app/not-found.tsx` exists and is not empty
2. `grep -n "export default function NotFound" src/app/not-found.tsx` returns a match
3. `grep -n 'href="/"' src/app/not-found.tsx` returns a match
4. `npm run lint` passes with no errors
</verification>

<success_criteria>
CRIT-01 is satisfied when: A request to any unmatched URL (e.g., /does-not-exist) renders the branded not-found page with a visible heading and a working link back to the homepage, all within the site's Header/Footer layout.
</success_criteria>

<output>
After completion, create `.planning/phases/01-critical-infrastructure/01-01-SUMMARY.md` using the summary template.
</output>
