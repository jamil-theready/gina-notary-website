---
phase: 02-schema-structured-data
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/app/blog/page.tsx
  - src/app/contact/page.tsx
  - src/app/service-areas/page.tsx
  - src/app/notary-services/page.tsx
  - src/app/service-areas/[slug]/page.tsx
  - src/app/blog/[slug]/page.tsx
  - src/app/notary-services/[slug]/page.tsx
autonomous: true
requirements:
  - SCHEMA-04
  - SCHEMA-06

must_haves:
  truths:
    - "blog/page.tsx has no localBusinessSchema script tag"
    - "contact/page.tsx has no localBusinessSchema script tag"
    - "service-areas/page.tsx has no localBusinessSchema script tag"
    - "notary-services/page.tsx has no localBusinessSchema script tag"
    - "service-areas/[slug]/page.tsx has no inline localBusinessSchema object — removed entirely"
    - "blog/[slug]/page.tsx renders a BreadcrumbList JSON-LD script tag"
    - "notary-services/[slug]/page.tsx renders a BreadcrumbList JSON-LD script tag"
    - "service-areas/[slug]/page.tsx renders a BreadcrumbList JSON-LD script tag"
  artifacts:
    - path: "src/app/blog/page.tsx"
      provides: "Blog listing page — no standalone LocalBusiness schema"
    - path: "src/app/contact/page.tsx"
      provides: "Contact page — no standalone LocalBusiness schema"
    - path: "src/app/service-areas/page.tsx"
      provides: "Service areas listing page — no standalone LocalBusiness schema"
    - path: "src/app/notary-services/page.tsx"
      provides: "Services listing page — no standalone LocalBusiness schema"
    - path: "src/app/service-areas/[slug]/page.tsx"
      provides: "Service area detail — BreadcrumbList only, no duplicate LocalBusiness"
    - path: "src/app/blog/[slug]/page.tsx"
      provides: "Blog post — BreadcrumbList added"
    - path: "src/app/notary-services/[slug]/page.tsx"
      provides: "Service page — BreadcrumbList added"
  key_links:
    - from: "src/app/blog/[slug]/page.tsx"
      to: "src/lib/schema.ts"
      via: "breadcrumbSchema import"
      pattern: "breadcrumbSchema"
    - from: "src/app/notary-services/[slug]/page.tsx"
      to: "src/lib/schema.ts"
      via: "breadcrumbSchema import"
      pattern: "breadcrumbSchema"
    - from: "src/app/service-areas/[slug]/page.tsx"
      to: "src/lib/schema.ts"
      via: "breadcrumbSchema import"
      pattern: "breadcrumbSchema"
---

<objective>
Remove duplicate LocalBusiness schema blocks from 4 landing pages and the service-area slug page. Add BreadcrumbList JSON-LD to the 3 dynamic detail page types (blog posts, service pages, service area pages).

Purpose: The global layout.tsx already injects LocalBusiness schema on every page. Duplicate blocks on individual pages waste parser cycles and can confuse Google. Breadcrumbs help search engines understand site hierarchy and generate rich results.
Output: 7 page files updated. No standalone LocalBusiness blocks anywhere except layout.tsx. BreadcrumbList on all detail pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-schema-structured-data/02-01-SUMMARY.md

<interfaces>
<!-- breadcrumbSchema signature from plan 02-01 output -->
From src/lib/schema.ts (after plan 02-01):
```typescript
export function breadcrumbSchema(
  items: { name: string; url: string }[]
): object
// Returns: { "@context": "https://schema.org", "@type": "BreadcrumbList", itemListElement: [...] }
```

<!-- What each dynamic page currently imports from schema.ts -->
blog/[slug]/page.tsx imports: { blogPostSchema, faqSchema }
notary-services/[slug]/page.tsx imports: { serviceSchema, faqSchema }
service-areas/[slug]/page.tsx imports: { faqSchema }
// All three need breadcrumbSchema added to their import

<!-- Current duplicate schema blocks to REMOVE (exact patterns) -->
blog/page.tsx line 3: import { localBusinessSchema } from "@/lib/schema";
blog/page.tsx lines 21-26: <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(localBusinessSchema()) }} />

contact/page.tsx line 4: import { localBusinessSchema } from "@/lib/schema";
contact/page.tsx lines 35-40: <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(localBusinessSchema()) }} />

service-areas/page.tsx line 4: import { localBusinessSchema } from "@/lib/schema";
service-areas/page.tsx lines 33-38: <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(localBusinessSchema()) }} />

notary-services/page.tsx line 4: import { localBusinessSchema } from "@/lib/schema";
notary-services/page.tsx lines 63-70 (approx): <script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(localBusinessSchema()) }} />

service-areas/[slug]/page.tsx lines 52-75: entire inline const localBusinessSchema = { ... } object definition
service-areas/[slug]/page.tsx lines 79-82: <script> block rendering localBusinessSchema (the inline const)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove duplicate localBusinessSchema from 4 landing pages</name>
  <files>
    src/app/blog/page.tsx,
    src/app/contact/page.tsx,
    src/app/service-areas/page.tsx,
    src/app/notary-services/page.tsx
  </files>
  <read_first>
    src/app/blog/page.tsx,
    src/app/contact/page.tsx,
    src/app/service-areas/page.tsx,
    src/app/notary-services/page.tsx
  </read_first>
  <action>
For each of the 4 files, make two edits:

1. Remove the localBusinessSchema import line:
   `import { localBusinessSchema } from "@/lib/schema";`
   (if schema.ts still has other needed imports on the same line, keep those)

2. Remove the script block that renders localBusinessSchema():
   ```tsx
   <script
     type="application/ld+json"
     dangerouslySetInnerHTML={{
       __html: JSON.stringify(localBusinessSchema()),
     }}
   />
   ```

In blog/page.tsx: the import is on its own line (line 3). Remove it. The script block wraps the entire page return — it is the first element inside the fragment. Remove the script tag; keep everything else.

In contact/page.tsx: import is on line 4. Remove it. Script block is first element in the return fragment. Remove it; keep the rest of the JSX.

In service-areas/page.tsx: import is on line 4. Remove it. Script block is first element in the return fragment. Remove it.

In notary-services/page.tsx: import is on line 4. Remove it. Script block is first element in the return fragment. Remove it.

Do NOT remove any other imports or JSX. Do NOT touch the localBusinessSchema import in layout.tsx — that one is the keeper.
  </action>
  <verify>
    <automated>grep -l "localBusinessSchema" /Users/admin/Desktop/Clients/Gina\ Notary/website/src/app/blog/page.tsx /Users/admin/Desktop/Clients/Gina\ Notary/website/src/app/contact/page.tsx /Users/admin/Desktop/Clients/Gina\ Notary/website/src/app/service-areas/page.tsx /Users/admin/Desktop/Clients/Gina\ Notary/website/src/app/notary-services/page.tsx 2>/dev/null || echo "PASS: no matches"</automated>
  </verify>
  <acceptance_criteria>
    - grep returns "PASS: no matches" or finds zero files (none of the 4 files contain "localBusinessSchema")
    - npm run build exits 0
  </acceptance_criteria>
  <done>All 4 landing pages compile without localBusinessSchema import or script tag. Layout.tsx remains the sole source of global LocalBusiness schema.</done>
</task>

<task type="auto">
  <name>Task 2: Remove inline LocalBusiness and add BreadcrumbList to service-areas/[slug]/page.tsx</name>
  <files>src/app/service-areas/[slug]/page.tsx</files>
  <read_first>src/app/service-areas/[slug]/page.tsx</read_first>
  <action>
Edit src/app/service-areas/[slug]/page.tsx:

1. Add breadcrumbSchema to the import from "@/lib/schema":
   Change: `import { faqSchema } from "@/lib/schema";`
   To: `import { faqSchema, breadcrumbSchema } from "@/lib/schema";`

2. Remove the entire inline localBusinessSchema const block (lines ~52-75):
```typescript
const localBusinessSchema = {
  "@context": "https://schema.org",
  "@type": "LocalBusiness",
  name: "Gina Gonzalez Notary Public",
  ...
  priceRange: "$",
};
```
Delete this entire object — do not keep any part of it.

3. Remove the <script> block that renders the inline localBusinessSchema:
```tsx
<script
  type="application/ld+json"
  dangerouslySetInnerHTML={{ __html: JSON.stringify(localBusinessSchema) }}
/>
```
Delete this script tag only. Keep the faqSchema script tag below it.

4. Add a breadcrumbSchema script block immediately before the faqSchema script block (inside the return fragment, before the first <section>):
```tsx
<script
  type="application/ld+json"
  dangerouslySetInnerHTML={{
    __html: JSON.stringify(
      breadcrumbSchema([
        { name: "Home", url: "https://www.ginagonzaleznotary.com/" },
        { name: "Service Areas", url: "https://www.ginagonzaleznotary.com/service-areas/" },
        { name: area.name, url: `https://www.ginagonzaleznotary.com/service-areas/${slug}/` },
      ])
    ),
  }}
/>
```

The final return should contain: breadcrumbSchema script, faqSchema script, then the Hero section and remaining JSX. No localBusinessSchema script.
  </action>
  <verify>
    <automated>grep -n "localBusinessSchema\|BreadcrumbList\|breadcrumbSchema\|Gina Gonzalez Notary Public\|priceRange.*\$\"" /Users/admin/Desktop/Clients/Gina\ Notary/website/src/app/service-areas/\[slug\]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep finds "breadcrumbSchema" in the file (import and usage)
    - grep finds NO "localBusinessSchema" in the file
    - grep finds NO "Gina Gonzalez Notary Public" (the wrong name from the old inline schema)
    - grep finds NO 'priceRange.*"$"' (the wrong priceRange from the old inline schema)
    - npm run build exits 0
  </acceptance_criteria>
  <done>service-areas/[slug]/page.tsx has BreadcrumbList JSON-LD with 3 items (Home > Service Areas > City). No inline LocalBusiness schema remains. faqSchema script is preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Add BreadcrumbList to blog/[slug]/page.tsx and notary-services/[slug]/page.tsx</name>
  <files>
    src/app/blog/[slug]/page.tsx,
    src/app/notary-services/[slug]/page.tsx
  </files>
  <read_first>
    src/app/blog/[slug]/page.tsx,
    src/app/notary-services/[slug]/page.tsx
  </read_first>
  <action>
**blog/[slug]/page.tsx:**

1. Add breadcrumbSchema to the schema import:
   Change: `import { blogPostSchema, faqSchema } from "@/lib/schema";`
   To: `import { blogPostSchema, faqSchema, breadcrumbSchema } from "@/lib/schema";`

2. Add a BreadcrumbList script block inside the return fragment, immediately before the existing blogPostSchema script block:
```tsx
<script
  type="application/ld+json"
  dangerouslySetInnerHTML={{
    __html: JSON.stringify(
      breadcrumbSchema([
        { name: "Home", url: "https://www.ginagonzaleznotary.com/" },
        { name: "Blog", url: "https://www.ginagonzaleznotary.com/blog/" },
        { name: post.title, url: canonicalUrl },
      ])
    ),
  }}
/>
```

---

**notary-services/[slug]/page.tsx:**

1. Add breadcrumbSchema to the schema import:
   Change: `import { serviceSchema, faqSchema } from "@/lib/schema";`
   To: `import { serviceSchema, faqSchema, breadcrumbSchema } from "@/lib/schema";`

2. Add a BreadcrumbList script block inside the return fragment, immediately before the existing serviceSchema script block:
```tsx
<script
  type="application/ld+json"
  dangerouslySetInnerHTML={{
    __html: JSON.stringify(
      breadcrumbSchema([
        { name: "Home", url: "https://www.ginagonzaleznotary.com/" },
        { name: "Services", url: "https://www.ginagonzaleznotary.com/notary-services/" },
        { name: service.shortTitle || service.title, url: `https://www.ginagonzaleznotary.com/notary-services/${slug}/` },
      ])
    ),
  }}
/>
```

Do NOT modify any other logic, JSX, or imports in either file.
  </action>
  <verify>
    <automated>grep -n "breadcrumbSchema" /Users/admin/Desktop/Clients/Gina\ Notary/website/src/app/blog/\[slug\]/page.tsx /Users/admin/Desktop/Clients/Gina\ Notary/website/src/app/notary-services/\[slug\]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep finds "breadcrumbSchema" in blog/[slug]/page.tsx (import line and usage)
    - grep finds "breadcrumbSchema" in notary-services/[slug]/page.tsx (import line and usage)
    - npm run build exits 0
    - No TypeScript errors on breadcrumbSchema call sites
  </acceptance_criteria>
  <done>blog/[slug]/page.tsx and notary-services/[slug]/page.tsx each render a BreadcrumbList JSON-LD block with Home > parent > current page hierarchy. Build passes.</done>
</task>

</tasks>

<verification>
Run the following checks after all 3 tasks complete:

1. Confirm no duplicate LocalBusiness on landing pages:
   `grep -rl "localBusinessSchema" src/app/blog/page.tsx src/app/contact/page.tsx src/app/service-areas/page.tsx src/app/notary-services/page.tsx`
   Expected: no output (all 4 files are clean)

2. Confirm breadcrumbs on all 3 detail page types:
   `grep -l "breadcrumbSchema" src/app/blog/[slug]/page.tsx src/app/notary-services/[slug]/page.tsx src/app/service-areas/[slug]/page.tsx`
   Expected: all 3 files listed

3. Confirm service area slug page no longer has inline LocalBusiness:
   `grep "Gina Gonzalez Notary Public\|priceRange.*\"\$\"" src/app/service-areas/[slug]/page.tsx`
   Expected: no output

4. Full build:
   `npm run build`
   Expected: exit 0, no TypeScript errors
</verification>

<success_criteria>
1. blog/page.tsx, contact/page.tsx, service-areas/page.tsx, notary-services/page.tsx contain no localBusinessSchema import or script block
2. service-areas/[slug]/page.tsx has no inline localBusinessSchema const — the object with name "Gina Gonzalez Notary Public" and priceRange "$" is gone
3. All 3 detail page types (blog/[slug], notary-services/[slug], service-areas/[slug]) render a BreadcrumbList with correct hierarchy
4. npm run build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-schema-structured-data/02-02-SUMMARY.md`
</output>
